# Models

The follwoing script contain wrappers for Scikit-Learn models. 

Models include: 
    
- Logistic Regression (lr)
- K-Nearest Neghbors (knn)
- Random Forest (rf)
- C-Support Vector (svc)
- XGBoost (xgb)
- Keras (mlp)